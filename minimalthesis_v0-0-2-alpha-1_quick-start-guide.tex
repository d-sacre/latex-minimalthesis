\documentclass{./class/minimalthesis}

\usepackage{blindtext}

% sans serif
% \usepackage{tgtermes}
% \renewcommand*\familydefault{\sfdefault}
% fira sans + newtxsf for math
%	\usepackage[sfdefault]{FiraSans} %% option 'sfdefault' activates Fira Sans as the default text font
%	\usepackage{newtxsf}

%
%\usepackage{cmbright} % better math

% broken ttfamily/verb, but math looks good
%	\usepackage{libertine}
%	\usepackage{libertinust1math} 

% looks good with math
%	\usepackage[sc]{mathpazo}
%	\linespread{1.05} 

% favorite for sans serif
% math + text looks good, but issue with ttfamily/verb
%\usepackage{newtxtext}
%\usepackage[uprightGreek]{newtxmath} % verify wether uprightGreek option is necessary
%\renewcommand*\familydefault{\sfdefault}

 
\def\releaseVersion{0.0.2-alpha-1}


\mtSetupComplete{
	thesis = {
		title = minimalthesis,
		subtitle = {Quick-Start-Guide (v\releaseVersion)},
		author = {
			name = Daniel Sacré
		}
	},
	visual styling = {
		default font = sans-serif,
		color scheme = {
			type = fancy-colorful
		},
		caption/propagate color scheme = true
	},
	modules = {mathematics, physics, chemistry, coding, data visualization},
	bibliography = {
		file path = ./example-bibliograhy_biber.bib
	},
	pdfmetadata = {
		subject = {A Quick-Start-Guide for the "minimalthesis" LaTeX Documentclass, Version \releaseVersion.},
		keywords = {academic-thesis, template, latex, documentclass, minimalthesis, v\releaseVersion, quick-start-guide, doc. rev. 0}
	},
	titlepage = {
		logo = {
			object type = image,
			file path = ./class/img/minimalthesis_logo.pdf,
			height = 8 cm % 10 cm
		},
		handed in by = {text = written by}
	},
%	debug/show margins=true
}

\begin{document}
	\mtGenerateTitlepage
	\mtGenerateTOC
	\input{./quick-start-guide_content/minimalthesis_v0-0-2-alpha-1.tex}
	\mtGenerateBibliography
\end{document}